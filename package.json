{
  "name": "ikeepmy",
  "version": "1.0.144",
  "description": "Types for admin, api, mobile app ",
  "scripts": {
    "build": "rm -rf dist && node_modules/.bin/tsc && npm run upversion && npm publish",
    "upversion": "npm version patch -git-tag-version false"
  },
  "author": "",
  "license": "ISC",
  "main": "./dist",
  "devDependencies": {
    "@types/node": "^18.11.9",
    "tslib": "^2.4.0",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@types/axios": "^0.14.0"
  }
}
